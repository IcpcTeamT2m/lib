#include <vector>


vector<char> v(配列数, 初期値);
vector<char> v(可変 配列数);

int a;
a = v.size(); //vの配列数がaに戻る　//forに有用的

v.push_back(5); // 末尾に値5を追加

v.erase(v.begin() + 1);  //vの配列の始めから1番目を消去



//例

sort(v.begin(), v.end());
sort(v.begin(), v.begin() + 3);



//サブルーチン

void sub(int a, vector<char> &v)
{
	//配列変更がメインにも適用される
}
int main()
{
	int a = 9;

	vector<char> v(10);
	sub(a, v);
}
